name: carbon_demo
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.9
  - pip                   
  - geopandas
  - ipykernel
  - ipyleaflet
  - ipywidgets==8.0.2
  - contextily
  - pyarrow
  - pip:
      - rasterio==1.4.0
      - urbanity==0.5.2    # your custom pinned package
      # any other pip packages
prefix: /Users/winston/miniconda3/envs/carbon_demo
